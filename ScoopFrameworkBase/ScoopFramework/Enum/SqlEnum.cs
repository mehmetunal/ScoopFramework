namespace ScoopFramework.Enum
{
    public enum SqlQueryEnum
    {

        SELECT,
        FROM,
        WHERE,
        AND,
        INSERT,
        INTO,
        Delete,
        Update,
        SET,
        VALUES,
        OR
    }
}
